/* Brilo Details - backend config.
 * Paste your Supabase Project URL and anon (public) key below to turn on
 * shared, real-time data across all devices. While these are placeholders,
 * the app runs in local mode (data stays on this device).
 */
window.BRILO_CONFIG = {
  supabaseUrl: "YOUR_SUPABASE_URL",
  supabaseAnonKey: "YOUR_SUPABASE_ANON_KEY"
};
